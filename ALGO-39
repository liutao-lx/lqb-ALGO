#include <iostream>
using namespace std;
#include<algorithm>
#include<set> 
int main()
{
	set<int> s;
	set<int>::iterator it;
	for(int i=0;i<10;i++){
		int a;
		cin>>a;
		s.insert(a);
	}
	for(it=s.begin();it!=s.end();it++){
		cout<<*it<<endl;
	}
    return 0;
}
